# Environment-variable template only; never commit real credentials.
# Future configuration will cover server settings, MongoDB, authentication secrets,
# CORS origins, Cloudinary, Razorpay, WhatsApp, secure cookies/tokens, and logging.
# config/env.js will eventually validate and expose these values to backend modules.

# Resend Email Integration
RESEND_API_KEY=re_123456789
EMAIL_FROM=Majedaar Restaurant <onboarding@resend.dev>

# Web Push VAPID Configuration
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
VAPID_SUBJECT=mailto:admin@majedaar.com

