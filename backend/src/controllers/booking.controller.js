/*
 * Responsibility: Handle table-booking HTTP requests for customers and administrators.
 * Future work: Delegate creation, lookup, status changes, and management rules to booking.service.js
 * after validation and authorization.
 * Integration: booking.routes.js invokes this controller; the service uses Booking and Customer models.
 */
