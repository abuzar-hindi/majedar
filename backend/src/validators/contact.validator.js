/*
 * Responsibility: Define validation and sanitization rules for contact and support messages.
 * Future work: Limit content, normalize contact fields, and reject unsafe input before persistence.
 * Integration: contact.routes.js applies these rules through validation.middleware.js.
 */
