/*
 * Responsibility: Define validation rules for guest and customer order requests.
 * Future work: Validate line items, variants, quantities, contact details, and delivery/pickup data;
 * sanitize input before trusted totals are calculated by order services.
 * Integration: order.routes.js applies these rules before order.controller.js.
 */
