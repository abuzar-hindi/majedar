/*
 * Responsibility: Define the future Mongoose model for payment records.
 * Future work: Track provider identifiers, order links, amounts, statuses, failures, refunds,
 * verification metadata, and webhook events without exposing secrets.
 * Integration: Razorpay and verification services will update it; orders and admin reporting
 * will consume its status through controllers.
 */