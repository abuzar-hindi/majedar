/*
 * Responsibility: Declare future contact and support-message endpoints.
 * Future work: Connect routes to contact.controller.js with validation, sanitization, and rate limiting.
 * Integration: app.js mounts this router; the controller delegates persistence and admin workflows.
 */
